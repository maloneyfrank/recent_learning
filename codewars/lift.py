class Dinglemouse(object):

    def __init__(self, queues, capacity):
        self.queues = queues
        self.capacity = capacity
        self.riders = []
        self.floors = len(queues[0])
        self.current_floor = 0
        self.direction = 'up'
        
    def theLift(self):   
        up_riders = [i, q for i, q in enumerate(self.queues) if q > i]
        
        if self.direction == 'up':
            if(self.queues)
        return []class Dinglemouse(object):

    def __init__(self, queues, capacity):
        self.queues = queues
        self.capacity = capacity
        self.riders = []
        self.floors = len(queues[0])
        self.current_floor = 0
        self.direction = 'up'
        
    def theLift(self):   
        up_riders = [i, q for i, q in enumerate(self.queues) if q > i]
        
        if self.direction == 'up':
            if(self.queues)
        return []
